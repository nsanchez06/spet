import styled from 'styled-components';

export const FooterWrapper = styled.footer`
  .footerContent {
    background-color: black;
    color: hsl(0deg, 0%, 57%);
    padding: 0;
    text-align: center;
    width: 100%;
    }
`;